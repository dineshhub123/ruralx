import { FirstLiPipePipe } from './first-li-pipe.pipe';

describe('FirstLiPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstLiPipePipe();
    expect(pipe).toBeTruthy();
  });
});
